{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capabilities (least privilege: core IPC + open external URLs)",
  "windows": ["main"],
  "permissions": ["core:default", "opener:allow-open-url"]
}
