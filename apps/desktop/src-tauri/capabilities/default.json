{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capabilities (least privilege: core IPC only)",
  "windows": ["main"],
  "permissions": ["core:default"]
}
